/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '', // or '/your-base-path' if you need one
  // other configuration options...
}

module.exports = nextConfig
